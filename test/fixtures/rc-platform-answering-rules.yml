---
swagger: '2.0'
info:
  title: RingCentral Answering Rules API
  description: Call Handling Settings API
  version: 1.0.34
  contact:
    name: RingCentral Connect Platform
    url: http://developers.ringcentral.com
    email: platform@ringcentral.com
  license:
    name: RingCentral API License Agreement and Terms of Use
    url: https://www.ringcentral.com/legal/apilitos.html
host: platform.ringcentral.com
schemes:
- https
basePath: "/restapi"
produces:
- application/json
tags:
- name: "Business Hours"
- name: "Incoming Call Information"
- name: "Caller Blocking"
- name: "Forwarding Numbers"
- name: "Answering Rules"
- name: "Greetings"
- name: "IVR"
x-auth-required: true
x-service-version: "v1"
x-service-interface: "rest"
x-service-name: "pas"
x-internal-api: false
x-blacklisting-strategy: "Off"
x-metered-api: true
x-metering-group: "System"
paths:

  /v1.0/account/{accountId}/extension/{extensionId}/business-hours:
    get:
      tags:
      - "Business Hours"
      summary: "Get User Business Hours"
      operationId: "loadUserBusinessHours"
      description: "<p style='font-style:italic;'>Since 1.0.15 (Release 7.0)</p><p>Returns
        the extension user hours when answering rules are to be applied.</p><h4>Required
        Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>ReadAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr></tbody></table><h4>API
        Group</h4><p>Light</p>"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        type: "string"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to indicate
          the account logged-in within the current session"
        required: true
        default: "~"
      - name: "extensionId"
        type: "string"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate the
          extension assigned to the account logged-in within the current session"
        required: true
        default: "~"
      responses:
        '200':
          description: "Success"
          schema:
            "$ref": "#/definitions/UserBusinessHours"
      x-api-group: "extension/business-hours"
      x-throttling-group: "Light"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadExtensions"
    put:
      tags:
      - "Business Hours"
      summary: "Update User Business Hours"
      operationId: "updateUserBusinessHours"
      description: "<p style='font-style:italic;'>Since 1.0.28 (Release 8.4)</p><p>Updates
        the extension user hours when answering rules are to be applied.</p><h4>Required
        Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>EditExtensions</td><td>Viewing and updating user extension info
        (includes extension name, number, email and phone number, assigned phone numbers,
        devices and other extension settings)</td></tr></tbody></table><h4>API Group</h4><p>Medium</p>"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        type: "string"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to indicate
          the account logged-in within the current session"
        required: true
        default: "~"
      - name: "extensionId"
        type: "string"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate the
          extension assigned to the account logged-in within the current session"
        required: true
        default: "~"
      - name: "body"
        in: "body"
        description: "JSON body"
        schema:
          "$ref": "#/definitions/UserBusinessHoursUpdateRequest"
        required: true
      responses:
        '200':
          description: "Success"
          schema:
            "$ref": "#/definitions/UserBusinessHoursUpdate"      
      x-api-group: "extension/business-hours"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserAnsweringRules"                  
  /v1.0/account/{accountId}/business-hours:
     get:
       tags:
       - "Business Hours"
       summary: "Get User Business Hours"
       description: "<p style='font-style:italic;'>Since 1.0.15 (Release 7.0)</p><p>Returns\
         \ the extension user hours when answering rules are to be applied.</p><h4>Usage\
         \ Plan Group</h4><p>Light</p>"
       operationId: "loadUserBusinessHours"
       consumes:
       - "application/json"
       produces:
       - "application/json"
       parameters:
       - name: "accountId"
         in: "path"
         required: true
         type: "integer"
         default: "~"
       - in: "body"
         name: "body"
         required: false
         schema:
           $ref: "#/definitions/AnsweringRulesEndpoint"
       responses:
         200:
           description: "Success"
           schema:
             $ref: "#/definitions/GetUserBusinessHoursResponse"
       x-api-group: "account/business-hours"
       x-throttling-group: "Light"
       x-metered-api: true
       x-metering-group: "System"
       x-app-permission: "ReadAccounts"
       x-user-permission: "ReadExtensions"
     put:
       tags:
       - Business Hours
       summary: Update User Business Hours
       operationId: updateUserBusinessHours
       description: "<p style='font-style:italic;'>Since 1.0.28 (Release 8.4)</p><p>Updates
         the extension user hours when answering rules are to be applied.</p><h4>Required
         Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
         class='code'>EditExtensions</td><td>Viewing and updating user extension info
         (includes extension name, number, email and phone number, assigned phone numbers,
         devices and other extension settings)</td></tr></tbody></table><h4>API Group</h4><p>Medium</p>"
       consumes:
       - application/json
       produces:
       - application/json
       parameters:
       - name: accountId
         type: string
         in: path
         description: Internal identifier of a RingCentral account or tilde (~) to
           indicate the account logged-in within the current session
         required: true
         default: "~"
       - name: body
         in: body
         description: JSON body
         schema:
           "$ref": "#/definitions/UserBusinessHoursUpdateRequest"
         required: true
       responses:
         '200':
           description: Success
           schema:
             "$ref": "#/definitions/UserBusinessHoursUpdate"
       x-api-group: "account/business-hours"
       x-throttling-group: "Medium"
       x-metered-api: true
       x-metering-group: "System"
       x-app-permission: "EditExtensions"
       x-user-permission: "EditUserAnsweringRules"
       
  /v1.0/account/{accountId}/extension/{extensionId}/incoming-call-info:
    get:
      tags:
      - "Incoming Call Information"
      consumes:
      - application/json
      produces:
      - application/json
      summary: Get Extension Incoming Call Settings
      parameters:
      - in: path
        required: true
        type: string
        name: accountId
      - in: path
        required: true
        type: string
        name: extensionId
      responses:
        '200':
          description: Success
      x-throttling-group: Medium
      x-app-permission: ReadAccounts
      x-user-permission: ReadUserInfo
      x-api-group: extension/incoming-call-info
    put:
      tags:
      - "Incoming Call Information"
      consumes:
      - application/json
      produces:
      - application/json
      summary: Update Extension Incoming Call Settings
      parameters:
      - in: path
        required: true
        type: string
        name: accountId
      - in: path
        required: true
        type: string
        name: extensionId
      responses:
        '200':
          description: Success
      x-throttling-group: Medium
      x-app-permission: EditExtensions
      x-user-permission: EditCallInfo
      x-api-group: extension/incoming-call-info
 
  /v1.0/account/{accountId}/extension/{extensionId}/blocked-number:
   get:
     tags:
     - "Caller Blocking"
     summary: "Get Blocked Number List"
     description: "<p style='font-style:italic;'></p><p></p><h4>Required Permissions</h4><table\
       \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
       \ class='code'>ReadAccounts</td><td>Viewing user account info (including name,\
       \ business name, address and phone number/account number)</td></tr></tbody></table><h4>Usage\
       \ Plan Group</h4><p>Light</p>"
     operationId: "listBlockedNumbers"
     consumes:
     - "application/json"
     produces:
     - "application/json"
     parameters:
     - name: "accountId"
       in: "path"
       description: "Internal identifier of a RingCentral account or tilde (~) to\
         \ indicate the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "extensionId"
       in: "path"
       description: "Internal identifier of an extension or tilde (~) to indicate\
         \ the extension assigned to the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "page"
       in: "query"
       required: false
       type: "string"
       default: "1"
     - name: "perPage"
       in: "query"
       required: false
       type: "string"
       default: "100"
     - in: "body"
       name: "body"
       required: false
       schema:
         $ref: "#/definitions/BlockedNumbersEndpoint"
     responses:
       200:
         description: "Success"
         schema:
           $ref: "#/definitions/"
     x-api-group: "extension/blocked-number"
     x-throttling-group: "Light"
     x-metered-api: true
     x-metering-group: "System"
     x-app-permission: "ReadAccounts"
     x-user-permission: "ReadBlockedNumbers"
   post:
     tags:
     - "Caller Blocking"
     summary: "Add Blocked Number"
     description: "<p style='font-style:italic;'></p><p></p><h4>Required Permissions</h4><table\
       \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
       \ class='code'>EditExtensions</td><td>Viewing and updating my extension info\
       \ (includes extension name, number, email and phone number)</td></tr></tbody></table><h4>Usage\
       \ Plan Group</h4><p>Medium</p>"
     operationId: "blockNumber"
     consumes:
     - "application/json"
     produces:
     - "application/json"
     parameters:
     - name: "accountId"
       in: "path"
       description: "Internal identifier of a RingCentral account or tilde (~) to\
         \ indicate the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "extensionId"
       in: "path"
       description: "Internal identifier of an extension or tilde (~) to indicate\
         \ the extension assigned to the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - in: "body"
       name: "body"
       required: false
       schema:
         $ref: "#/definitions/BlacklistEntryResource"
     responses:
       200:
         description: "Success"
         schema:
           $ref: "#/definitions/"
     x-api-group: "extension/blocked-number"
     x-throttling-group: "Medium"
     x-metered-api: true
     x-metering-group: "System"
     x-app-permission: "EditExtensions"
     x-user-permission: "EditBlockedNumbers"
  /v1.0/account/{accountId}/extension/{extensionId}/blocked-number/{blockedNumberId}:
   get:
     tags:
     - "Caller Blocking"
     summary: "Get Blocked Number(s) by ID"
     description: "<p style='font-style:italic;'></p><p></p><h4>Required Permissions</h4><table\
       \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
       \ class='code'>ReadAccounts</td><td>Viewing user account info (including name,\
       \ business name, address and phone number/account number)</td></tr></tbody></table><h4>Usage\
       \ Plan Group</h4><p>Light</p>"
     operationId: "loadBlockedNumber"
     consumes:
     - "application/json"
     produces:
     - "application/json"
     - "multipart/mixed"
     parameters:
     - name: "accountId"
       in: "path"
       description: "Internal identifier of a RingCentral account or tilde (~) to\
         \ indicate the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "extensionId"
       in: "path"
       description: "Internal identifier of an extension or tilde (~) to indicate\
         \ the extension assigned to the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "blockedNumberId"
       in: "path"
       description: "Internal identifiers of a blocked number list entry"
       required: true
       type: "integer"
     responses:
       200:
         description: "Success"
         schema:
           $ref: "#/definitions/"
     x-api-group: "extension/blocked-number"
     x-throttling-group: "Light"
     x-metered-api: true
     x-metering-group: "System"
     x-app-permission: "ReadAccounts"
     x-user-permission: "ReadBlockedNumbers"
   put:
     tags:
     - "Caller Blocking"
     summary: "Update Blocked Number(s) by ID"
     description: "<p style='font-style:italic;'></p><p></p><h4>Required Permissions</h4><table\
       \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
       \ class='code'>EditExtensions</td><td>Viewing and updating my extension info\
       \ (includes extension name, number, email and phone number)</td></tr></tbody></table><h4>Usage\
       \ Plan Group</h4><p>Medium</p>"
     operationId: "updateBlockedNumber"
     consumes:
     - "application/json"
     produces:
     - "application/json"
     - "multipart/mixed"
     parameters:
     - name: "accountId"
       in: "path"
       description: "Internal identifier of a RingCentral account or tilde (~) to\
         \ indicate the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "extensionId"
       in: "path"
       description: "Internal identifier of an extension or tilde (~) to indicate\
         \ the extension assigned to the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "blockedNumberId"
       in: "path"
       description: "Internal identifier of a blocked number list entry"
       required: true
       type: "integer"
     - in: "body"
       name: "body"
       required: false
       schema:
         $ref: "#/definitions/BlacklistEntryResource"
     responses:
       200:
         description: "Success"
         schema:
           $ref: "#/definitions/"
     x-api-group: "extension/blocked-number"
     x-throttling-group: "Medium"
     x-metered-api: true
     x-metering-group: "System"
     x-app-permission: "EditExtensions"
     x-user-permission: "EditBlockedNumbers"
   delete:
     tags:
     - "Caller Blocking"
     summary: "Delete Blocked Number(s) by ID"
     description: "<p style='font-style:italic;'></p><p></p><h4>Required Permissions</h4><table\
       \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
       \ class='code'>EditExtensions</td><td>Viewing and updating my extension info\
       \ (includes extension name, number, email and phone number)</td></tr></tbody></table><h4>Usage\
       \ Plan Group</h4><p>Medium</p>"
     operationId: "unblockNumber"
     consumes:
     - "application/json"
     produces:
     - "application/json"
     - "multipart/mixed"
     parameters:
     - name: "accountId"
       in: "path"
       description: "Internal identifier of a RingCentral account or tilde (~) to\
         \ indicate the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "extensionId"
       in: "path"
       description: "Internal identifier of an extension or tilde (~) to indicate\
         \ the extension assigned to the account logged-in within the current session"
       required: true
       type: "integer"
       default: "~"
     - name: "blockedNumberId"
       in: "path"
       description: "Internal identifiers of a blocked number list entry"
       required: true
       type: "integer"
     responses:
       200:
         description: "Success"
         schema:
           $ref: "#/definitions/"
     x-api-group: "extension/blocked-number"
     x-throttling-group: "Medium"
     x-metered-api: true
     x-metering-group: "System"
     x-app-permission: "EditExtensions"
     x-user-permission: "EditBlockedNumbers"
  /v1.0/account/{accountId}/extension/{extensionId}/caller-blocking:
    get:
      tags:
      - Caller Blocking
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - name: "accountId"
          in: "path"
          required: true
          type: "string"
        - name: "extensionId"
          in: "path"
          required: true
          type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking"
      x-throttling-group: "Light"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadBlockedNumbers"
    put:
      tags:
      - Caller Blocking
      consumes:
        - "application/json"
      produces:
        - "application/json"
      parameters:
        - name: "accountId"
          in: "path"
          required: true
          type: "string"
        - name: "extensionId"
          in: "path"
          required: true
          type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking"
      x-throttling-group: "Light"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditBlockedNumbers"
  /v1.0/account/{accountId}/extension/{extensionId}/caller-blocking/phone-numbers:
    get:
      tags:
      - Caller Blocking
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      - name: "page"
        in: "query"
        required: false
        type: "integer"
      - name: "perPage"
        in: "query"
        required: false
        type: "integer"
      - name: "status"
        in: "query"
        required: false
        type: "string"
        enum:
          - "Blocked"
          - "Allowed"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking/phone-numbers"
      x-throttling-group: "Light"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadBlockedNumbers"
    post:
      tags:
      - Caller Blocking
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking/phone-numbers"
      x-throttling-group: "Medium"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditBlockedNumbers"
  /v1.0/account/{accountId}/extension/{extensionId}/caller-blocking/phone-numbers/{blockedNumberId}:
    get:
      tags:
      - Caller Blocking
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      - name: "blockedNumberId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking/phone-numbers"
      x-throttling-group: "Light"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadBlockedNumbers"
    delete:
      tags:
      - Caller Blocking
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      - name: "blockedNumberId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking/phone-numbers"
      x-throttling-group: "Medium"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditBlockedNumbers"
    put:
      tags:
      - Caller Blocking
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      - name: "blockedNumberId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "extension/caller-blocking/phone-numbers"
      x-throttling-group: "Medium"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditBlockedNumbers"

  /v1.0/account/{accountId}/extension/{extensionId}/forwarding-number:
    get:
      tags:
      - "Forwarding Numbers"
      summary: "Get Extension Forwarding Number List"
      description: "<p style='font-style:italic;'>Since 1.0.7 (Release 5.16)</p><p>Returns\
        \ the list of extension phone numbers used for call forwarding and call flip.\
        \ The returned list contains all the extension phone numbers that are used\
        \ for call forwarding and call flip.</p><h4>Required Permissions</h4><table\
        \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>ReadAccounts</td><td>Viewing user account info (including name,\
        \ business name, address and phone number/account number)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Light</p>"
      operationId: "listExtensionForwardingNumbers"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "page"
        in: "query"
        description: "Indicates the page number to retrieve. Only positive number\
          \ values are allowed. Default value is '1'"
        required: false
        type: "integer"
      - name: "perPage"
        in: "query"
        description: "Indicates the page size (number of items). If not specified,\
          \ the value is '100' by default"
        required: false
        type: "integer"
      - in: "body"
        name: "body"
        required: false
        schema:
          $ref: "#/definitions/ExtensionForwardingNumbersEndpoint"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/GetExtensionForwardingNumberListResponse"
      x-api-group: "extension/forwarding-number"
      x-throttling-group: "Light"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadUserForwardingFlipNumbers"
    post:
      tags:
      - "Forwarding Numbers"
      summary: "Create Forwarding Number"
      description: "<p style='font-style:italic;'>Since 1.0.12 (Release 6.4)</p><p>Adds\
        \ a new forwarding number to the forwarding number list.</p><h4>Required Permissions</h4><table\
        \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>EditExtensions</td><td>Viewing and updating user extension\
        \ info (includes extension name, number, email and phone number, assigned\
        \ phone numbers, devices and other extension settings)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Medium</p>"
      operationId: "createExtensionForwardingNumber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - in: "body"
        name: "body"
        description: "JSON body"
        required: true
        schema:
          $ref: "#/definitions/CreateForwardingNumberRequest"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/ForwardingNumberInfo"
      x-api-group: "extension/forwarding-number"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserForwardingFlipNumbers"
  /v1.0/account/{accountId}/extension/{extensionId}/forwarding-number/{forwardingNumberId}:
    get:
      tags:
      - "Forwarding Numbers"
      operationId: "getForwardingNumber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "forwardingNumberId"
        in: "path"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        required: false
        schema:
          $ref: "#/definitions/ExtensionForwardingNumbersEndpoint"
      - name: "extensionId"
        in: "path"
        required: true
        type: "string"
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        required: false
        schema:
          $ref: "#/definitions/ExtensionEndpoint"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/ForwardingNumberResource"
          headers: {}
      x-api-group: "extension/forwarding-number"
      x-throttling-group: "Light"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadUserForwardingFlipNumbers"
    put:
      tags:
      - "Forwarding Numbers"
      summary: "Update Forwarding Number by ID"
      description: "<p style='font-style:italic;'>Since 1.0.24 (Release 8.0)</p><p>Updates\
        \ an existent forwarding number from the forwarding number list.</p><h4>Required\
        \ Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>EditExtensions</td><td>Viewing and updating user extension\
        \ info (includes extension name, number, email and phone number, assigned\
        \ phone numbers, devices and other extension settings)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Medium</p>"
      operationId: "updateExtensionForwardingNumber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "forwardingNumberId"
        in: "path"
        description: "Internal identifier of a forwarding number; returned in response\
          \ in the id field"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        description: "JSON body"
        required: true
        schema:
          $ref: "#/definitions/UpdateForwardingNumberRequest"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/ForwardingNumberInfo"
      x-api-group: "extension/forwarding-number"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserForwardingFlipNumbers"
    delete:
      tags:
      - "Forwarding Numbers"
      summary: "Delete Forwarding Number by ID"
      description: "<p style='font-style:italic;'>Since 1.0.24 (Release 8.0)</p><p>Deletes\
        \ a forwarding number from the forwarding number list by its ID.</p><h4>Required\
        \ Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>EditExtensions</td><td>Viewing and updating user extension\
        \ info (includes extension name, number, email and phone number, assigned\
        \ phone numbers, devices and other extension settings)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Medium</p>"
      operationId: "deleteExtensionForwardingNumber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "forwardingNumberId"
        in: "path"
        description: "Internal identifier of a forwarding number"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        required: false
        schema:
          $ref: "#/definitions/ExtensionForwardingNumbersEndpoint"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/"
      x-api-group: "extension/forwarding-number"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserForwardingFlipNumbers"
     
  /v1.0/account/{accountId}/extension/{extensionId}/answering-rule:
    get:
      tags:
      - "Answering Rules"
      summary: "Get Answering Rules List"
      description: "<p style='font-style:italic;'>Since 1.0.15 (Release 7.0)</p><p>Returns\
        \ the extension answering rules.</p><h4>Required Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>ReadAccounts</td><td>Viewing user account info (including name,\
        \ business name, address and phone number/account number)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Light</p>"
      operationId: "loadAnsweringRulesList"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "page"
        in: "query"
        required: false
        type: "string"
        default: "1"
      - name: "perPage"
        in: "query"
        required: false
        type: "string"
        default: "100"
      - in: "body"
        name: "body"
        required: false
        schema:
          $ref: "#/definitions/AnsweringRulesEndpoint"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/"
      x-api-group: "extension/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadUserAnsweringRules"
    post:
      tags:
      - "Answering Rules"
      summary: "Create Answering Rule"
      description: "<p style='font-style:italic;'>Since 1.0.24 (Release 8.0)</p><p>Creates\
        \ a custom answering rule for a particular caller ID.</p><h4>Required Permissions</h4><table\
        \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>EditExtensions</td><td>Viewing and updating my extension info\
        \ (includes extension name, number, email and phone number)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Medium</p>"
      operationId: "createAnsweringRuleInfo"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - in: "body"
        name: "body"
        description: "JSON body"
        required: true
        schema:
          $ref: "#/definitions/CreateAnsweringRuleRequest"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/AnsweringRuleInfo"
      x-api-group: "extension/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserAnsweringRules"    
  /v1.0/account/{accountId}/extension/{extensionId}/answering-rule/{ruleId}:
    get:
      tags:
      - "Answering Rules"
      summary: "Get Answering Rule by ID"
      description: "<p style='font-style:italic;'>Since 1.0.15 (Release 7.0)</p><p>Returns\
        \ an answering rule by ID.</p><h4>Required Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>ReadAccounts</td><td>Viewing user account info (including name,\
        \ business name, address and phone number/account number)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Light</p>"
      operationId: "loadAnsweringRuleInfo"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "ruleId"
        in: "path"
        description: "Internal identifier of an answering rule. The value can be standard\
          \ digital ID or specific ID - either business-hours-rule or after-hours-rule"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/AnsweringRuleInfo"
      x-api-group: "extension/answering-rule"
      x-throttling-group: "Light"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadUserAnsweringRules"
    put:
      tags:
      - "Answering Rules"
      summary: "Update Answering Rule by ID"
      description: "<p style='font-style:italic;'>Since 1.0.24 (Release 8.0)</p><p>Updates\
        \ a custom answering rule for a particular caller ID.</p><h4>Required Permissions</h4><table\
        \ class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td\
        \ class='code'>EditExtensions</td><td>Viewing and updating my extension info\
        \ (includes extension name, number, email and phone number)</td></tr></tbody></table><h4>Usage\
        \ Plan Group</h4><p>Medium</p>"
      operationId: "updateAnsweringRuleInfo"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "ruleId"
        in: "path"
        description: "Internal identifier of an answering rule"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        description: "JSON body"
        required: true
        schema:
          $ref: "#/definitions/UpdateAnsweringRuleRequest"
      responses:
        200:
          description: "Success"
          schema:
            $ref: "#/definitions/AnsweringRuleInfo"
      x-api-group: "extension/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserAnsweringRules"
    delete:
      tags:
      - "Answering Rules"
      operationId: "deleteAnsweringRule"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "string"
        default: "~"
      - name: "ruleId"
        in: "path"
        description: "Internal identifier of an answering rule"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        description: "JSON body"
        required: true
        schema:
          $ref: "#/definitions/UpdateAnsweringRuleRequest"
      responses:
        default:
          description: "successful operation"
      x-api-group: "extension/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserAnsweringRules" 
      
  /v1.0/account/{accountId}/answering-rule:
    post:
      tags:
      - Answering Rules
      summary: Create Company Answering Rule
      operationId: createCompanyAnsweringRuleInfo
      description: "<p style='font-style:italic;'>Since 1.0.31 (Release 9.2)</p><p>Creates
        a company answering rule for a particular caller ID.</p><h4>Required Permissions</h4><table
        class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>ReadAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr><tr><td class='code'>EditExtensions</td><td>Viewing
        and updating user extension info (includes extension name, number, email and
        phone number, assigned phone numbers, devices and other extension settings)</td></tr><tr><td
        class='code'>EditAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr></tbody></table><h4>API
        Group</h4><p>Medium</p>"
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - name: accountId
        type: string
        in: path
        description: Internal identifier of a RingCentral account or tilde (~) to
          indicate the account logged-in within the current session
        required: true
        default: "~"
      - name: body
        in: body
        description: JSON body
        schema:
          "$ref": "#/definitions/CompanyAnsweringRuleRequest"
        required: true
      responses:
        '200':
          description: Success
          schema:
            "$ref": "#/definitions/CompanyAnsweringRuleInfo"
      x-api-group: "account/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "EditCompanyAnsweringRules"
    get:
      tags:
      - Answering Rules
      summary: Get Company Answering Rule List
      operationId: listCompanyAnsweringRule
      description: "<p style='font-style:italic;'>Since 1.0.31 (Release 9.2)</p><p>Returns
        a list of company answering rules.</p><h4>Required Permissions</h4><table
        class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>ReadAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr></tbody></table><h4>API
        Group</h4><p>Medium</p>"
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - name: accountId
        type: string
        in: path
        description: Internal identifier of a RingCentral account or tilde (~) to
          indicate the account logged-in within the current session
        required: true
        default: "~"
      responses:
        '200':
          description: Success
          schema:
            "$ref": "#/definitions/CompanyAnsweringRuleList"
      x-api-group: "account/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadCompanyAnsweringRules"
  /v1.0/account/{accountId}/answering-rule/{ruleId}:
    get:
      tags:
      - Answering Rules
      summary: Get Company Answering Rule by ID
      operationId: loadCompanyAnsweringRuleInfo
      description: "<p style='font-style:italic;'>Since 1.0.31 (Release 9.2)</p><p>Returns
        a company answering rule by ID.</p><h4>Required Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>ReadAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr></tbody></table><h4>API
        Group</h4><p>Light</p>"
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - name: accountId
        type: string
        in: path
        description: Internal identifier of a RingCentral account or tilde (~) to
          indicate the account logged-in within the current session
        required: true
        default: "~"
      - name: ruleId
        type: string
        in: path
        description: Internal identifier of an answering rule. The value can be standard
          digital ID or specific ID - either business-hours-rule or after-hours-rule
        required: true
      responses:
        '200':
          description: Success
          schema:
            "$ref": "#/definitions/CompanyAnsweringRuleInfo"
      x-api-group: "account/answering-rule"
      x-throttling-group: "Light"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadCompanyAnsweringRules"
    put:
      tags:
      - Answering Rules
      summary: Update Company Answering Rule
      operationId: updateCompanyAnsweringRuleInfo
      description: "<p style='font-style:italic;'>Since 1.0.31 (Release 9.2)</p><p>Updates
        a company answering rule.</p><h4>Required Permissions</h4><table class='fullwidth'><thead><tr><th>Permission</th><th>Description</th></tr></thead><tbody><tr><td
        class='code'>ReadAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr><tr><td class='code'>EditExtensions</td><td>Viewing
        and updating user extension info (includes extension name, number, email and
        phone number, assigned phone numbers, devices and other extension settings)</td></tr><tr><td
        class='code'>EditAccounts</td><td>Viewing user account info (including name,
        business name, address and phone number/account number)</td></tr></tbody></table><h4>API
        Group</h4><p>Medium</p>"
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - name: accountId
        type: string
        in: path
        description: Internal identifier of a RingCentral account or tilde (~) to
          indicate the account logged-in within the current session
        required: true
        default: "~"
      - name: ruleId
        type: string
        in: path
        description: Internal identifier of an answering rule. The value can be standard
          digital ID or specific ID - either business-hours-rule or after-hours-rule
        required: true
      - name: body
        in: body
        description: JSON body
        schema:
          "$ref": "#/definitions/CompanyAnsweringRuleUpdate"
        required: true
      responses:
        '200':
          description: Success
          schema:
            "$ref": "#/definitions/CompanyAnsweringRuleInfo"
      x-api-group: "account/answering-rule"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "EditCompanyAnsweringRules"
  
  /v1.0/dictionary/greeting:
    get:
      tags:
      - Greetings
      operationId: "getGreetings"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "page"
        in: "query"
        description: "Indicates the page number to retrieve. Only positive number\
          \ values are allowed. Default value is '1'"
        required: false
        type: "integer"
      - name: "perPage"
        in: "query"
        description: "Indicates the page size (number of items). If not specified,\
          \ the value is '100' by default"
        required: false
        type: "integer"
      - name: "type"
        in: "query"
        description: ""
        required: false
        type: "string"
      responses:
        200:
          description: "successful operation"
      x-api-group: "dictionary/greeting"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
  /v1.0/dictionary/greeting/{greetingId}:
    get:
      tags:
      - Greetings
      operationId: "getGreeting"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "greetingId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
      x-api-group: "dictionary/greeting"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"

  /v1.0/account/{accountId}/greeting:
      post:
        tags:
        - Greetings
        operationId: "createGreeting"
        consumes:
        - "application/json"
        produces:
        - "application/json"
        parameters:
        - name: "accountId"
          in: "path"
          description: "Internal identifier of a RingCentral account or tilde (~) to\
            \ indicate the account logged-in within the current session"
          required: true
          type: "integer"
          default: "~"
        - in: "body"
          name: "body"
          required: false
        responses:
          default:
            description: "successful operation"
        x-api-group: "extension/greeting"
        x-throttling-group: "Heavy"
        x-metered-api: true
        x-metering-group: "System"
        x-app-permission: "EditExtensions"
        x-user-permission: "EditUserAnsweringRules"
  /v1.0/account/{accountId}/greeting/{greetingId}:
        get:
          tags:
          - Greetings
          operationId: "getGreeting"
          consumes:
          - "application/json"
          produces:
          - "application/json"
          parameters:
          - name: "accountId"
            in: "path"
            description: "Internal identifier of a RingCentral account or tilde (~) to\
              \ indicate the account logged-in within the current session"
            required: true
            type: "integer"
            default: "~"
          - name: "greetingId"
            in: "path"
            description: ""
            required: true
            type: "integer"
          - in: "body"
            name: "body"
            required: false
          responses:
            default:
              description: "successful operation"
          x-api-group: "extension/greeting"
          x-throttling-group: "Medium"
          x-metered-api: true
          x-metering-group: "System"
          x-app-permission: "ReadAccounts"
          x-user-permission: "ReadUserInfo"

  /v1.0/account/{accountId}/extension/{extensionId}/greeting:
    post:
      tags:
      - Greetings
      operationId: "createGreeting"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - in: "body"
        name: "body"
        required: false
      responses:
        default:
          description: "successful operation"
      x-api-group: "extension/greeting"
      x-throttling-group: "Heavy"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "EditUserAnsweringRules"
  /v1.0/account/{accountId}/extension/{extensionId}/greeting/{greetingId}:
    get:
      tags:
      - Greetings
      operationId: "getGreeting"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        description: "Internal identifier of a RingCentral account or tilde (~) to\
          \ indicate the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "extensionId"
        in: "path"
        description: "Internal identifier of an extension or tilde (~) to indicate\
          \ the extension assigned to the account logged-in within the current session"
        required: true
        type: "integer"
        default: "~"
      - name: "greetingId"
        in: "path"
        description: ""
        required: true
        type: "integer"
      - in: "body"
        name: "body"
        required: false
      responses:
        default:
          description: "successful operation"
      x-api-group: "extension/greeting"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadUserInfo"

  /v1.0/account/{accountId}/ivr:
    get:
      tags:
      - IVR
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "body"
        in: "body"
        required: true
        schema:
          $ref: "#/definitions/IvrResource"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr"
      x-throttling-group: "Medium"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadCompanyInfo"
    put:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "body"
        in: "body"
        required: true
        schema:
          $ref: "#/definitions/IvrResource"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr"
      x-throttling-group: "Medium"
      x-app-permission: "EditAccounts"
      x-user-permission: "AutoReceptionist"
  /v1.0/account/{accountId}/ivr-prompts:
    post:
      tags:
      - IVR      
      consumes:
      - "multipart/form-data"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-prompts"
      x-throttling-group: "Heavy"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "EditCompanyGreetings"
    get:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-prompts"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadCompanyGreetings"
  /v1.0/account/{accountId}/ivr-prompts/{promptId}:
    get:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "promptId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-prompts"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "ReadCompanyGreetings"
    delete:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "promptId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-prompts"
      x-throttling-group: "Heavy"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "EditCompanyGreetings"
    put:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "promptId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-prompts"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "EditCompanyGreetings"
  /v1.0/account/{accountId}/ivr-menus:
    get:
      tags:
      - IVR
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-menus"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "AutoReceptionist"
    post:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-menus"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditAccounts"
      x-user-permission: "AddRemoveUsers"
  /v1.0/account/{accountId}/ivr-menus/{ivrMenuId}:
    put:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "ivrMenuId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-menus"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "EditExtensions"
      x-user-permission: "AutoReceptionist"
    get:
      tags:
      - IVR      
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "accountId"
        in: "path"
        required: true
        type: "string"
      - name: "ivrMenuId"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "Success"
      x-api-group: "account/ivr-menus"
      x-throttling-group: "Medium"
      x-metered-api: true
      x-metering-group: "System"
      x-app-permission: "ReadAccounts"
      x-user-permission: "AutoReceptionist"

definitions:
  BlockedNumbersEndpoint:
    type: "object"
    properties:
      multiPartHandler:
        $ref: "#/definitions/MultiPartHandler"
      subscriberInfo:
        $ref: "#/definitions/SubscriptionInfo" 

  AnsweringRulesEndpoint:
    type: "object"
    properties:
      multiPartHandler:
        $ref: "#/definitions/MultiPartHandler"
      businessHours:
        $ref: "#/definitions/BusinessHoursResource"
      subscriberInfo:
        $ref: "#/definitions/SubscriptionInfo"
        
  GetUserBusinessHoursResponse:
    properties:
      uri:
        type: "string"
        description: "Canonical URI of a business-hours resource"
      schedule:
        description: "Schedule when an answering rule is applied"
        $ref: "#/definitions/ScheduleInfoUserBusinessHours"
  ScheduleInfoUserBusinessHours:
    properties:
      weeklyRanges:
        description: "Weekly schedule"
        $ref: "#/definitions/WeeklyScheduleInfo"
        
  BlacklistEntryResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      id:
        type: "string"
      phoneNumber:
        type: "string"
      name:
        type: "string"
    xml:
      name: "blacklistedNumber"
        
  BlacklistEntriesResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      records:
        type: "array"
        xml:
          name: "records"
          wrapped: true
        items:
          $ref: "#/definitions/BlacklistEntryResource"
      paging:
        $ref: "#/definitions/Paging"
      navigation:
        $ref: "#/definitions/Navigation"
    xml:
      name: "blacklist"
      
  GetExtensionForwardingNumberListResponse:
    properties:
      records:
        type: "array"
        description: "List of forwarding phone numbers"
        items:
          $ref: "#/definitions/ForwardingNumberInfo"
      navigation:
        description: "Information on navigation"
        $ref: "#/definitions/NavigationInfo"
      paging:
        description: "Information on paging"
        $ref: "#/definitions/PagingInfo"      
        
  CreateForwardingNumberRequest:
    properties:
      phoneNumber:
        type: "string"
        description: "Forwarding/Call flip phone number"
      label:
        type: "string"
        description: "Forwarding/Call flip number title"
  UpdateForwardingNumberRequest:
    properties:
      phoneNumber:
        type: "string"
        description: "Forwarding/Call flip phone number"
      label:
        type: "string"
        description: "Forwarding/Call flip number title"
      flipNumber:
        type: "string"
        description: "Number assigned to the call flip phone number, corresponds to\
          \ the shortcut dial number"
        
  CreateAnsweringRuleRequest:
    required:
    - "name"
    - "type"
    properties:
      type:
        type: "string"
        description: "Type of an answering rule. The 'Custom' value should be specified"
      name:
        type: "string"
        description: "Name of an answering rule specified by user"
      callers:
        type: "array"
        description: "Answering rule will be applied when calls are received from\
          \ the specified caller(s)"
        items:
          $ref: "#/definitions/CallersInfoRequest"
      forwarding:
        description: "Forwarding parameters. Should be specified if the 'callHandlingAction'\
          \ parameter value is set to 'ForwardCalls'. These settings determine the\
          \ forwarding numbers to which the call should be forwarded"
        $ref: "#/definitions/ForwardingInfoCreateRuleRequest"
  CallersInfoRequest:
    properties:
      callerId:
        type: "string"
        description: "Phone number of a caller"
      name:
        type: "string"
        description: "Contact name of a caller"
  ForwardingInfoCreateRuleRequest:
    properties:
      rules:
        type: "array"
        description: "Information on a call forwarding rule"
        items:
          $ref: "#/definitions/RuleInfoCreateRuleRequest"
  RuleInfoCreateRuleRequest:
    properties:
      index:
        type: "integer"
        description: "Forwarding number (or group) ordinal"
      ringCount:
        type: "integer"
        description: "Number of rings for a forwarding number (or group)"
      forwardingNumbers:
        type: "array"
        description: "Forwarding number (or group) data"
        items:
          $ref: "#/definitions/ForwardingNumberInfoRulesCreateRuleRequest"
  ForwardingNumberInfoRulesCreateRuleRequest:
    properties:
      id:
        type: "string"
        description: "Internal identifier of a forwarding number"
  UpdateAnsweringRuleRequest:
    properties:
      forwarding:
        description: "Forwarding parameters. Returned if 'ForwardCalls' is specified\
          \ in 'callHandlingAction'. These settings determine the forwarding numbers\
          \ to which the call will be forwarded"
        $ref: "#/definitions/ForwardingInfoCreateRuleRequest"

  IvrResource:
    type: "object"
    required: [ "mode" ]
    properties:
      mode:
        type: "string"
        enum:
        - "SingleLevel"
        - "MultiLevel"
      topMenu:
        $ref: "#/definitions/IvrMenuResource"
      actions:
        type: "array"
        items:
          $ref: "#/definitions/IvrKeyPressResource"
  IvrMenuResource:
    type: "object"
    properties:
      name:
        type: "string"
      extensionNumber:
        type: "string"
      prompt:
        $ref: "#/definitions/PromptResource"
      actions:
        type: "array"
        items:
          $ref: "#/definitions/IvrKeyPressResource"
  IvrKeyPressResource:
    type: "object"
    properties:
      input:
        type: "string"
        enum:
        - "1"
        - "2"
        - "3"
        - "4"
        - "5"
        - "6"
        - "7"
        - "8"
        - "9"
        - "0"
        - "Hash"
        - "Star"
        - "NoInput"
      action:
        type: "string"
        enum:
        - "Connect"
        - "Voicemail"
        - "DialByName"
        - "Transfer"
        - "Disconnect"
        - "Repeat"
        - "ReturnToRoot"
        - "ReturnToPrevious"
  PromptResource:
    type: "object"
    properties:
      mode:
        type: "string"
        enum:
        - "Audio"
        - "TextToSpeech"
      audio:
        $ref: "#/definitions/IdResource"
      text:
        type: "string"
        maxLength: 1000
      language:
        $ref: "#/definitions/PromptLanguageResource"
  PromptLanguageResource:
    type: "object"
    properties:
      name:
        type: "string"
      localeCode:
        type: "string"

  BusinessHoursResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      schedule:
        $ref: "#/definitions/ScheduleResource"
    xml:
      name: "versionedResource"
      
  AnsweringRuleInfo:
    properties:
      uri:
        type: "string"
        description: "Canonical URI to the answering rule resource"
      id:
        type: "string"
        description: "Internal identifier of an asnwering rule"
      type:
        type: "string"
        description: "Type of an answering rule"
        enum:
        - "BusinessHours"
        - "AfterHours"
        - "Custom"
      name:
        type: "string"
        description: "Name of an answering rule specified by user"
      enabled:
        type: "boolean"
        description: "Specifies if an answering rule is active or inactive"
      schedule:
        description: "Schedule when an answering rule should be applied"
        $ref: "#/definitions/ScheduleInfo"
      calledNumbers:
        type: "array"
        description: "Answering rules are applied when calling to selected number(s)"
        items:
          $ref: "#/definitions/CalledNumberInfo"
      callers:
        type: "array"
        description: "Answering rules are applied when calls are received from specified\
          \ caller(s)"
        items:
          $ref: "#/definitions/CallersInfo"
      callHandlingAction:
        type: "string"
        description: "Specifies how incoming calls are forwarded"
        enum:
        - "ForwardCalls"
        - "TakeMessagesOnly"
        - "PlayAnnouncementOnly"
        - "UnconditionalForwarding"
      forwarding:
        description: "Forwarding parameters. Returned if 'ForwardCalls' is specified\
          \ in 'callHandlingAction'. These settings determine the forwarding numbers\
          \ to which the call will be forwarded"
        $ref: "#/definitions/ForwardingInfo"
      unconditionalForwarding:
        description: "Unconditional forwarding parameters. Returned if 'UnconditionalForwarding'\
          \ is specified in 'callHandlingAction'"
        $ref: "#/definitions/UnconditionalForwardingInfo"
      voicemail:
        description: "Specifies whether to take a voicemail and who should do it"
        $ref: "#/definitions/VoicemailInfo"
  ScheduleInfo:
    properties:
      weeklyRanges:
        description: "Weekly schedule"
        $ref: "#/definitions/WeeklyScheduleInfo"
      ranges:
        description: "Specific data ranges"
        $ref: "#/definitions/RangesInfo"
      ref:
        type: "string"
        description: "The user's schedule specified for business hours or after hours;\
          \ it can also be set/retrieved calling the corresponding method"
        enum:
        - "BusinessHours"
        - "AfterHours"
  WeeklyScheduleInfo:
    properties:
      monday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      tuesday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      wednesday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      thursday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      friday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      saturday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
      sunday:
        type: "array"
        description: "Time intervals for a particular day"
        items:
          $ref: "#/definitions/TimeInterval"
  RangesInfo:
    properties:
      from:
        type: "string"
        format: "date-time"
        description: "Date and time in format YYYY-MM-DD hh:mm"
      to:
        type: "string"
        format: "date-time"
        description: "Date and time in format YYYY-MM-DD hh:mm"
  TimeInterval:
    properties:
      from:
        type: "string"
        format: "date-time"
        description: "Time in format hh:mm"
      to:
        type: "string"
        format: "date-time"
        description: "Time in format hh:mm"
  CalledNumberInfo:
    properties:
      phoneNumber:
        type: "string"
        description: "Called phone number"
  CallersInfo:
    properties:
      callerId:
        type: "string"
        description: "Phone number of a caller"
      name:
        type: "string"
        description: "Contact name of a caller"
  ForwardingInfo:
    properties:
      notifyMySoftPhones:
        type: "boolean"
        description: "Specifies if the user's softphone(s) are notified before forwarding\
          \ the incoming call to desk phones and forwarding numbers"
      notifyAdminSoftPhones:
        type: "boolean"
        description: "Specifies if the administrator's softphone is notified before\
          \ forwarding the incoming call to desk phones and forwarding numbers. The\
          \ default value is 'False'"
      softPhonesRingCount:
        type: "integer"
        description: "Number of rings before forwarding starts"
      ringingMode:
        type: "string"
        description: "Specifies the order in which forwarding numbers ring. 'Sequentially'\
          \ means that forwarding numbers are ringing one at a time, in order of priority.\
          \ 'Simultaneously' means that forwarding numbers are ring all at the same\
          \ time"
        enum:
        - "Sequentially"
        - "Simultaneously"
      rules:
        type: "array"
        description: "Information on a call forwarding rule"
        items:
          $ref: "#/definitions/RuleInfo"
  RuleInfo:
    properties:
      index:
        type: "integer"
        description: "Forwarding number (or group) ordinal"
      ringCount:
        type: "integer"
        description: "Number of rings for a forwarding number (or group)"
      forwardingNumbers:
        type: "array"
        description: "Forwarding number (or group) data"
        items:
          $ref: "#/definitions/ForwardingNumberInfoRules"
  ForwardingNumberInfoRules:
    properties:
      uri:
        type: "string"
        description: "Link to a forwarding number resource"
      id:
        type: "string"
        description: "Internal identifier of a forwarding number"
      phoneNumber:
        type: "string"
        description: "Phone number to which the call is forwarded"
      label:
        type: "string"
        description: "Title of a forwarding number"
  UnconditionalForwardingInfo:
    properties:
      phoneNumber:
        type: "string"
        description: "Phone number to which the call is forwarded"
  VoicemailInfo:
    properties:
      enabled:
        type: "boolean"
        description: "If 'True' then voicemails are allowed to be received"
      recipient:
        description: "Recipient data"
        $ref: "#/definitions/RecipientInfo"
  RecipientInfo:
    properties:
      uri:
        type: "string"
        description: "Link to a recipient extension resource"
      id:
        type: "string"
        description: "Internal identifier of a recipient extension"
        
  AnsweringRulesResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      records:
        type: "array"
        xml:
          name: "records"
          wrapped: true
        items:
          $ref: "#/definitions/AnsweringRuleResource"
      paging:
        $ref: "#/definitions/Paging"
      navigation:
        $ref: "#/definitions/Navigation"
    xml:
      name: "collectionResource"

  AnsweringRuleResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      id:
        type: "string"
      type:
        type: "string"
        enum:
        - "BusinessHours"
        - "AfterHours"
        - "Custom"
      name:
        type: "string"
        minLength: 0
        maxLength: 30
      enabled:
        type: "boolean"
        default: false
      schedule:
        $ref: "#/definitions/ScheduleResource"
      callers:
        type: "array"
        items:
          $ref: "#/definitions/CallerResource"
      calledNumbers:
        type: "array"
        items:
          $ref: "#/definitions/PhoneNumberResource"
      callHandlingAction:
        type: "string"
        enum:
        - "ForwardCalls"
        - "TakeMessagesOnly"
        - "PlayAnnouncementOnly"
        - "UnconditionalForwarding"
        - "AgentQueue"
        - "Unknown"
      forwarding:
        $ref: "#/definitions/AnsweringForwardingResource"
      unconditionalForwarding:
        $ref: "#/definitions/UnconditionalForwardingResource"
      voicemail:
        $ref: "#/definitions/VoicemailSettingsResource"
    xml:
      name: "versionedResource"      
      
  ScheduleResource:
    type: "object"
    properties:
      ranges:
        type: "array"
        items:
          $ref: "#/definitions/DateTimeIntervalResource"
      weeklyRanges:
        type: "object"
        additionalProperties:
          type: "array"
          items:
            $ref: "#/definitions/IntervalResource"
      ref:
        type: "string"
        enum:
        - "BusinessHours"
        - "AfterHours"  

  CallerResource:
    type: "object"
    properties:
      callerId:
        type: "string"
        minLength: 0
        maxLength: 127
      name:
        type: "string"
        
  AnsweringForwardingResource:
    type: "object"
    properties:
      notifyMySoftPhones:
        type: "boolean"
        default: false
      notifyAdminSoftPhones:
        type: "boolean"
        default: false
      softPhonesRingCount:
        type: "integer"
        format: "int32"
      ringingMode:
        type: "string"
        enum:
        - "Sequentially"
        - "Simultaneously"
      rules:
        type: "array"
        items:
          $ref: "#/definitions/AnsweringForwardingRuleResource"
          
  UnconditionalForwardingResource:
    type: "object"
    properties:
      phoneNumber:
        type: "string"          
        
  VoicemailSettingsResource:
    type: "object"
    properties:
      enabled:
        type: "boolean"
        default: false
      recipient:
        $ref: "#/definitions/ExtensionResource"
        
  DateTimeIntervalResource:
    type: "object"
    properties:
      from:
        type: "string"
      to:
        type: "string"
        
  IntervalResource:
    type: "object"
    properties:
      from:
        type: "string"
      to:
        type: "string"      
        
  AnsweringForwardingRuleResource:
    type: "object"
    properties:
      index:
        type: "integer"
        format: "int32"
      ringCount:
        type: "integer"
        format: "int32"
      forwardingNumbers:
        type: "array"
        items:
          $ref: "#/definitions/ForwardingNumberResource"  

  ForwardingNumberResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      id:
        type: "string"
      phoneNumber:
        type: "string"
      label:
        type: "string"
      features:
        type: "array"
        xml:
          name: "feature"
          wrapped: true
        items:
          type: "string"
          enum:
          - "CallFlip"
          - "CallForwarding"
      flipNumber:
        type: "string"
        pattern: "[1-8]"
    xml:
      name: "forwardingNumberInfo"
      
  ForwardingNumbersResource:
    type: "object"
    properties:
      uri:
        type: "string"
        format: "uri"
        xml:
          attribute: true
      records:
        type: "array"
        xml:
          name: "records"
          wrapped: true
        items:
          $ref: "#/definitions/ForwardingNumberResource"
      paging:
        $ref: "#/definitions/Paging"
      navigation:
        $ref: "#/definitions/Navigation"
    xml:
      name: "forwardingNumberInfos"
      
  ForwardingNumberInfo:
    properties:
      id:
        type: "string"
        description: "Internal identifier of a forwarding/call flip phone number"
      uri:
        type: "string"
        description: "Canonical URI of a forwarding/call flip phone number"
      phoneNumber:
        type: "string"
        description: "Forwarding/Call flip phone number"
      label:
        type: "string"
        description: "Forwarding/Call flip number title"
      features:
        type: "string"
        description: "Type of option this phone number is used for. Multiple values\
          \ are accepted"
        enum:
        - "CallFlip"
        - "CallForwarding"
      flipNumber:
        type: "integer"
        description: "Number assigned to the call flip phone number, corresponds to\
          \ the shortcut dial number"      